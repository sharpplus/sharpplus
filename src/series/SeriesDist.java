package series;

public class SeriesDist {
	
	enum SeriesDistType {
		SUM,
		DTW,
		ERD,
		TWED
	}
	
//	public static long distance(long[][] x, long[][] y, LocalDistType localtype, SeriesDistType seriestype) {
//		if(seriestype == SeriesDistType.SUM) return sumDistance(x, y);
//		if(seriestype == SeriesDistType.DTW) return dtwDistance(x, y);
//		if(seriestype == SeriesDistType.ERD) return erdDistance(x, y);
//		return twedDistance(x, y);
//	}
//	
//	public static long sumDistance(long[][] x, long[][] y) {
//		
//	}
}
